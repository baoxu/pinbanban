# 拼板板 - 技术方案与时间估算

---

## 一、技术架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              拼板板技术架构                                  │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  前端层 (Frontend)                                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                       │
│  │  微信小程序  │  │   H5 页面     │  │  管理后台    │                       │
│  │  (Taro/原生) │  │  (Vue3)      │  │  (React)     │                       │
│  └──────────────┘  └──────────────┘  └──────────────┘                       │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  网关层 (Gateway)                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                      Nginx + API Gateway                             │   │
│  │              (负载均衡 / 限流 / 鉴权 / SSL)                          │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  服务层 (Backend Services)                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │ 用户服务 │ │ 订单服务 │ │ 拼单服务 │ │ 支付服务 │ │ 通知服务 │ │ 文件服务 │   │
│  │  Node.js│ │ Node.js │ │ Node.js │ │ Node.js │ │ Node.js │ │ Node.js │   │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘   │
│                                                                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                           │
│  │ AI 推荐  │ │ 图案服务 │ │ 物流服务 │ │ 数据统计 │                           │
│  │ Python  │ │ Node.js │ │ Node.js │ │ Node.js │                           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  数据层 (Data Layer)                                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐                   │
│  │    MySQL      │  │    Redis      │  │  MongoDB      │                   │
│  │  (订单/用户)  │  │  (缓存/会话)  │  │  (日志/行为)  │                   │
│  └───────────────┘  └───────────────┘  └───────────────┘                   │
│                                                                              │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐                   │
│  │  OSS(阿里云)  │  │  消息队列     │  │  Elasticsearch│                   │
│  │  (图片/图案)  │  │   RabbitMQ    │  │  (搜索)       │                   │
│  └───────────────┘  └───────────────┘  └───────────────┘                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术选型

| 模块 | 技术栈 | 选型理由 |
|------|--------|---------|
| 小程序 | Taro + React | 跨端、生态成熟、开发效率高 |
| H5 | Vue3 + Vite | 轻量、性能好、团队熟悉 |
| 管理后台 | React + Ant Design Pro | 组件丰富、开箱即用 |
| 后端框架 | NestJS (Node.js) | TypeScript、模块化、易维护 |
| AI 服务 | Python + FastAPI | ML 生态好、部署简单 |
| 数据库 | MySQL 8.0 | 成熟稳定、事务支持 |
| 缓存 | Redis 7.0 | 高性能、数据结构丰富 |
| 消息队列 | RabbitMQ | 可靠、支持延迟队列 |
| 对象存储 | 阿里云 OSS | 国内访问快、成本低 |
| 部署 | Docker + K8s | 容器化、弹性伸缩 |

---

## 二、数据库设计（核心表）

### 2.1 用户表 (users)

```sql
CREATE TABLE users (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  openid VARCHAR(64) UNIQUE NOT NULL,      -- 微信 openid
  nickname VARCHAR(64),                     -- 昵称
  avatar VARCHAR(255),                      -- 头像
  phone VARCHAR(20),                        -- 手机号
  height INT,                               -- 身高 (cm)
  weight INT,                               -- 体重 (kg)
  skill_level TINYINT,                      -- 滑雪水平 1-5
  style_preference VARCHAR(32),             -- 风格偏好
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_openid (openid)
);
```

### 2.2 板型表 (board_types)

```sql
CREATE TABLE board_types (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(64) NOT NULL,                -- 板型名称
  code VARCHAR(32) UNIQUE NOT NULL,         -- 板型编码
  category TINYINT NOT NULL,                -- 类别 1=全山 2=公园 3=野雪
  difficulty TINYINT,                       -- 难度 1-5
  base_price DECIMAL(10,2),                 -- 基础价格
  description TEXT,                         -- 描述
  images JSON,                              -- 图片数组
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 2.3 拼单表 (group_orders)

```sql
CREATE TABLE group_orders (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(32) UNIQUE NOT NULL,     -- 拼单号
  board_type_id BIGINT NOT NULL,            -- 板型 ID
  board_length INT NOT NULL,                -- 长度 (cm)
  waist_width INT NOT NULL,                 -- 板腰 (mm)
  sidecut_radius DECIMAL(4,1) NOT NULL,     -- 侧切半径 (m)
  binding_angle VARCHAR(16),                -- 固定器角度
  pattern_id BIGINT,                        -- 图案 ID
  target_count INT NOT NULL DEFAULT 3,      -- 目标人数
  current_count INT NOT NULL DEFAULT 1,     -- 当前人数
  status TINYINT NOT NULL DEFAULT 1,        -- 状态 1=拼单中 2=成功 3=失败
  expire_at TIMESTAMP NOT NULL,             -- 过期时间
  created_by BIGINT NOT NULL,               -- 创建人
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_status (status),
  INDEX idx_expire (expire_at)
);
```

### 2.4 拼单成员表 (group_order_members)

```sql
CREATE TABLE group_order_members (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  group_order_id BIGINT NOT NULL,           -- 拼单 ID
  user_id BIGINT NOT NULL,                  -- 用户 ID
  role TINYINT NOT NULL DEFAULT 2,          -- 角色 1=发起人 2=参与者
  deposit_paid BOOLEAN DEFAULT false,       -- 定金已付
  deposit_amount DECIMAL(10,2),             -- 定金金额
  final_price DECIMAL(10,2),                -- 最终价格
  status TINYINT NOT NULL DEFAULT 1,        -- 状态 1=待支付 2=已支付 3=已退出
  joined_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY idx_group_user (group_order_id, user_id)
);
```

### 2.5 订单表 (orders)

```sql
CREATE TABLE orders (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(32) UNIQUE NOT NULL,     -- 订单号
  group_order_id BIGINT,                    -- 拼单 ID
  user_id BIGINT NOT NULL,                  -- 用户 ID
  board_type_id BIGINT NOT NULL,            -- 板型 ID
  board_length INT NOT NULL,                -- 长度
  waist_width INT NOT NULL,                 -- 板腰
  sidecut_radius DECIMAL(4,1) NOT NULL,     -- 侧切
  binding_angle VARCHAR(16),                -- 固定器角度
  pattern_id BIGINT,                        -- 图案 ID
  total_amount DECIMAL(10,2) NOT NULL,      -- 总金额
  deposit_amount DECIMAL(10,2),             -- 定金
  final_amount DECIMAL(10,2),               -- 尾款
  status TINYINT NOT NULL DEFAULT 1,        -- 状态
  factory_id BIGINT,                        -- 工厂 ID
  production_status TINYINT,                -- 生产状态
  tracking_no VARCHAR(64),                  -- 物流单号
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  paid_at TIMESTAMP NULL,
  shipped_at TIMESTAMP NULL,
  completed_at TIMESTAMP NULL,
  INDEX idx_user (user_id),
  INDEX idx_status (status)
);
```

---

## 三、API 设计（核心接口）

### 3.1 拼单相关

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 创建拼单 | POST | /api/v1/group-orders | 创建新拼单 |
| 拼单列表 | GET | /api/v1/group-orders | 获取拼单列表 |
| 拼单详情 | GET | /api/v1/group-orders/:id | 获取拼单详情 |
| 加入拼单 | POST | /api/v1/group-orders/:id/join | 加入拼单 |
| 退出拼单 | POST | /api/v1/group-orders/:id/quit | 退出拼单 |
| 拼单匹配 | GET | /api/v1/group-orders/match | 获取推荐拼单 |

### 3.2 订单相关

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 创建订单 | POST | /api/v1/orders | 创建订单 |
| 订单列表 | GET | /api/v1/orders | 获取订单列表 |
| 订单详情 | GET | /api/v1/orders/:id | 获取订单详情 |
| 支付定金 | POST | /api/v1/orders/:id/deposit | 支付定金 |
| 支付尾款 | POST | /api/v1/orders/:id/final | 支付尾款 |
| 确认收货 | POST | /api/v1/orders/:id/confirm | 确认收货 |

### 3.3 用户相关

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 微信登录 | POST | /api/v1/auth/wechat | 微信登录 |
| 用户信息 | GET | /api/v1/users/me | 获取当前用户 |
| 更新档案 | PUT | /api/v1/users/me/profile | 更新滑雪档案 |
| 我的拼单 | GET | /api/v1/users/me/groups | 获取我的拼单 |

---

## 四、开发时间估算

### 4.1 MVP 版本（3 个月）

| 阶段 | 时间 | 工作内容 | 交付物 |
|------|------|---------|--------|
| **第 1 月** | 4 周 | 需求细化 + 技术选型 + 环境搭建 | 技术方案文档、开发环境 |
| 第 1 周 | - | 需求评审、原型确认 | PRD 终稿 |
| 第 2 周 | - | 技术选型、架构设计 | 技术方案文档 |
| 第 3 周 | - | 数据库设计、API 设计 | 数据库 ER 图、API 文档 |
| 第 4 周 | - | 开发环境、CI/CD 搭建 | 可运行的开发环境 |
| **第 2 月** | 4 周 | 核心功能开发 | 可测试的 MVP |
| 第 5 周 | - | 用户系统、微信登录 | 登录注册完成 |
| 第 6 周 | - | 板型配置、参数选择 | 定制流程完成 |
| 第 7 周 | - | 拼单系统、状态机 | 拼单功能完成 |
| 第 8 周 | - | 支付对接、订单管理 | 支付流程完成 |
| **第 3 月** | 4 周 | 测试 + 优化 + 上线 | 正式上线 |
| 第 9 周 | - | 内部测试、Bug 修复 | 测试报告 |
| 第 10 周 | - | 性能优化、压力测试 | 性能报告 |
| 第 11 周 | - | 小程序审核、灰度发布 | 审核通过 |
| 第 12 周 | - | 正式上线、监控部署 | 生产环境 |

**MVP 功能范围**：
- ✅ 微信小程序（定制 + 拼单 + 订单）
- ✅ 基础拼单功能（创建/加入/支付）
- ✅ 5 款基础板型、20 款基础图案
- ✅ 微信支付（定金 + 尾款）
- ✅ 简易管理后台
- ❌ AI 参数推荐（V2 版本）
- ❌ 板友匹配系统（V2 版本）
- ❌ 图案市场（V2 版本）

### 4.2 正式版（6 个月）

| 阶段 | 时间 | 工作内容 | 交付物 |
|------|------|---------|--------|
| **第 1-3 月** | - | MVP 开发（同上） | MVP 上线 |
| **第 4 月** | 4 周 | AI 功能开发 | AI 推荐上线 |
| 第 13 周 | - | 用户数据采集、模型训练 | 推荐模型 V1 |
| 第 14 周 | - | AI 参数推荐接口 | API 完成 |
| 第 15 周 | - | 前端集成、A/B 测试 | 功能上线 |
| **第 5 月** | 4 周 | 社交功能开发 | 社区上线 |
| 第 16 周 | - | 板友匹配系统 | 匹配算法 |
| 第 17 周 | - | 板友社区、动态 | 社区功能 |
| 第 18 周 | - | 分享裂变、邀请奖励 | 营销工具 |
| **第 6 月** | 4 周 | 图案市场 + 优化 | 完整版上线 |
| 第 19 周 | - | 图案市场、设计师入驻 | 市场上线 |
| 第 20 周 | - | 性能优化、监控完善 | 稳定性提升 |
| 第 21 周 | - | 压力测试、安全审计 | 安全报告 |
| 第 22 周 | - | 正式版发布、推广准备 | 正式版 |

### 4.3 人员配置

| 角色 | 人数 | 职责 |
|------|------|------|
| 产品经理 | 1 | 需求、原型、项目管理 |
| UI 设计师 | 1 | 界面设计、视觉规范 |
| 前端开发 | 2 | 小程序 + H5+ 后台 |
| 后端开发 | 2 | API 开发、数据库 |
| AI 工程师 | 1 (兼职) | 推荐算法 |
| 测试工程师 | 1 | 测试用例、质量保障 |

**合计**：7-8 人团队

---

## 五、成本估算

### 5.1 开发成本（6 个月）

| 项目 | 金额 | 说明 |
|------|------|------|
| 人力成本 | 80-120 万 | 7-8 人团队 × 6 个月 |
| 云服务器 | 5-8 万 | 阿里云/腾讯云 |
| 第三方服务 | 3-5 万 | 短信、OSS、CDN 等 |
| 设计外包 | 3-5 万 | UI/UX 设计（如外包） |
| **合计** | **90-140 万** | 含 3 个月缓冲 |

### 5.2 运营成本（首年）

| 项目 | 金额 | 说明 |
|------|------|------|
| 服务器运维 | 10-15 万 | 云资源 + 运维人力 |
| 营销推广 | 30-50 万 | 获客成本 |
| 客服团队 | 15-20 万 | 2-3 人客服 |
| **合计** | **55-85 万/年** | 首年运营 |

---

## 六、风险与建议

### 6.1 技术风险

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 拼单并发问题 | 中 | 高 | 分布式锁 + 消息队列 |
| 支付安全问题 | 低 | 高 | 正规支付渠道 + 审计 |
| 数据泄露 | 低 | 高 | 加密存储 + 权限控制 |
| 性能瓶颈 | 中 | 中 | 缓存 + 数据库优化 |

### 6.2 开发建议

1. **MVP 优先**：先上线核心功能，验证商业模式
2. **敏捷迭代**：2 周一个 Sprint，快速响应反馈
3. **数据驱动**：埋点统计，用数据指导优化
4. **供应链先行**：技术同时推进工厂合作
5. **法律合规**：提前咨询律师，规避风险

---

*技术方案版本：v1.0*  
*更新日期：2026-03-13*
